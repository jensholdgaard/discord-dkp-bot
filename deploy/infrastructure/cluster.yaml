# Cluster API – Hetzner Cluster definition
#
# This manifest creates a Kubernetes cluster on Hetzner Cloud using the
# Cluster API Hetzner provider (CAPH).
#
# Prerequisites:
#   - A management cluster with CAPH installed (see bootstrap.sh)
#   - Environment variables from clusterctl-settings.env
#
# Apply with: kubectl apply -f cluster.yaml
---
apiVersion: cluster.x-k8s.io/v1beta1
kind: Cluster
metadata:
  name: dkpbot-prod
  namespace: default
spec:
  clusterNetwork:
    pods:
      cidrBlocks:
        - "10.244.0.0/16"
    services:
      cidrBlocks:
        - "10.96.0.0/12"
  controlPlaneRef:
    apiVersion: controlplane.cluster.x-k8s.io/v1beta1
    kind: KubeadmControlPlane
    name: dkpbot-prod-control-plane
  infrastructureRef:
    apiVersion: infrastructure.cluster.x-k8s.io/v1beta1
    kind: HetznerCluster
    name: dkpbot-prod
---
apiVersion: infrastructure.cluster.x-k8s.io/v1beta1
kind: HetznerCluster
metadata:
  name: dkpbot-prod
  namespace: default
spec:
  controlPlaneRegions:
    - nbg1
  hcloudNetwork:
    enabled: true
    networkZone: eu-central
    cidrBlock: "10.0.0.0/16"
  sshKeys:
    hcloud:
      - name: dkpbot-ssh
  controlPlaneLoadBalancer:
    enabled: true
    region: nbg1
    type: lb11
    port: 6443
    algorithm:
      type: round_robin
  hetznerSecretRef:
    name: hetzner
    key:
      hcloudToken: hcloud
