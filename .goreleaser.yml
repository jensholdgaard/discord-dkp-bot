version: 2

project_name: dkpbot

before:
  hooks:
    - go mod tidy

builds:
  - id: dkpbot
    main: ./cmd/dkpbot
    binary: dkpbot
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X main.version={{.Version}}

dockers:
  - id: dkpbot
    ids:
      - dkpbot
    goos: linux
    goarch: amd64
    image_templates:
      - "ghcr.io/jensholdgaard/dkpbot:{{ .Tag }}"
      - "ghcr.io/jensholdgaard/dkpbot:latest"
    dockerfile: Dockerfile
    build_flag_templates:
      - "--label=org.opencontainers.image.created={{.Date}}"
      - "--label=org.opencontainers.image.title={{.ProjectName}}"
      - "--label=org.opencontainers.image.revision={{.FullCommit}}"
      - "--label=org.opencontainers.image.version={{.Version}}"

release:
  prerelease: auto
  mode: replace

archives:
  - id: default
    format: tar.gz
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^ci:"
